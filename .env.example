# Local SQLite (default). For cloud, set a Postgres URL and change prisma datasource provider to "postgresql".
DATABASE_URL="file:./dev.db"
# A long random string used to sign session cookies (e.g. `openssl rand -base64 32`)
AUTH_SECRET="change-me-to-a-long-random-string"
